import React from 'react'

const ListView = () => {
    //retrieve list from DB, store in local memory for modification sake, and post updates if necessary
    //if error in response, return an error screen with a button to go back to list view
    //error says private list, either log in or return to list view to select another
    //if order of list items is changed, items are removed, or items are added, show confirm screen after selecting save
    //users can add to list from any items they have saved to their library already
    
  return (
    <div>ListView</div>
  )
}

export default ListView